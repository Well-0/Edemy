{
  "name": "learning-platform",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron .",
    "dev": "tsc && electron .",
    "watch": "tsc --watch",
    "package": "npm run build && electron-builder"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1",
    "typescript": "^5.3.3",
    "@types/node": "^20.10.6"
  },
  "build": {
    "appId": "com.learningplatform.app",
    "files": [
      "dist/**/*",
      "index.html"
    ],
    "win": {
      "target": "nsis"
    }
  }
}